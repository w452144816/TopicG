# Direct dependencies, pinned. Verified on Python 3.13 / Windows 11.
# For an exact reproduction of the whole environment use requirements.lock.txt.
# (ASCII only: pip may read this file with a non-UTF-8 locale codec on Windows.)
gradio==6.27.0
anthropic==1.6.0
python-dotenv==1.2.3
pillow==12.3.0
openpyxl==3.1.5
